從V2.0.3開始

# V2.0.3: 
# 修掉find/replace中match的bug
# 去掉yoCrypt.py
# 避免更改highlighter時改到is_dirty
# 解決disable highlight 無效的問題
# 提示若輸入過密碼，還是restart電腦後才最安全，否則有傾印風險
# 建立sitemap && google3524e2fa87e535cd.html && robots.txt

# V2.0.4: 
# 說明: 官方網站更新速度會略慢於github
# 分開官網語言，分別加入home.md, encryption.md, features.md
# 修復 highlight as ... 改到 is_dirty 狀態的問題
# 修復 新視窗的字型大小問題
# 增進 Highlighter 的效率
# 解決 遇到emojihighlighter可能("數字元時")出現錯誤的問題
# 允許 google搜尋到官網
# 建立 _config.yml, 更新sitemap.xml && robots.txt
# 修正 .gitignore 的語法錯誤
# 加入 本文(development_log.txt)
# 修正 make_password.py
# 修正 password.txt 預設密碼為 "abc" (不含引號)
# 加入 官網圖示 && 名稱
# 建立 404.md 並美化官網

# V2.0.5
# change_master_password 解密失敗-取消更改-前面已經重新加密的會無法復原
# 改成先解密全部-錯誤則詢問使用者是否繼續-接著嘗試加密儲存-若失敗也詢問使用者是否繼續
# 若取消則把前面著加密重新儲存的檔案再解密復原
# 加入test_change_master_password.py，測試四種狀態下change_master_password的表現

# V3.0.0
修復某些輸入法沒有落實括號補全的問題
backspace可以同時刪除一組的連續括號(bracket)
當輸入左括號接著輸入右括號時，不會再出現重複的右括號了
加入是否啟用字動換行選項
增加初始化部分程式碼的可維護性
模組化init_ui函數
switch ("open" and "open crypted")'s shortcut
no more Ctrl+0 -> reset zoom
更正yoCryptCpp.py的路徑問題以及dll缺失問題
加速 find && replace 運作
修復輸入框跟分頁標籤不同步的問題
修復 find && replace 尋找數量不正確的問題
修正路徑，不再支援onefile -> 不向下相容
修正 find && replace 聚焦位置
find && replace 預設先尋找反白的文字
